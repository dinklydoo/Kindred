TODO (realistic)
    - better docs
    - I/O operations (idk if i want to support object printing ?? if not then its pretty trivial)
    - Function Currying (literally just closures with extra steps, but also not)
    - Inline optimization
    - Constant Folding
    - Unneccessary ref counting of static closures

TODO (in future probs not but if im so bored that i come back to this shit project):
    - maybe fix the language syntax because some of this shit is only there to prevent rr and sr conflicts ex.
        - func(:) -- empty params 
        - branches requiring comma seperation (,)
        ==> realistically i should include indentation in syntax but i really cba to deal with flex/yacc
    - Function subtyping (doable but enforcing it is annoying)
    - Handwrite parser with better error msgs
    - Lambda expressions (syntax is too annoying cbf to deal with yacc again but once i reduce to closure no extra work passed typechecking/reduction)
    - Linker (obv u could just modify to write globals and link using gcc but theres no dependency enforcement so :/ also main entrypoint is hardcoded lmao)
    - Maybe rewrite GC but probs not for this shit language (try hybrid nursery + rc)
    - Leaf function redzone optimization (pretty important for a functional language)